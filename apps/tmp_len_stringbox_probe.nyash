// Probe: StringBox.size() value path under JIT-direct
static box Main {
  main() {
    local s
    s = new StringBox("abc")
    local n
    n = s.size()
    print("len=" + n.toString())
    return n
  }
}

