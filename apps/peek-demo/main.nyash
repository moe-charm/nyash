// peek expression demo (P1: arms support blocks)
local animal = "cat"
local sound = peek animal {
  "dog" => { "bark" }
  "cat" => { local s = "meow" s }
  else => { "silent" }
}
local con = new ConsoleBox()
con.println(sound)
