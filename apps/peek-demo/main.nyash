// peek expression demo (P0: arms are single expressions)
local animal = "cat"
local sound = peek animal {
  "dog" => "bark"
  "cat" => "meow"
  else => "silent"
}
local con = new ConsoleBox()
con.println(sound)
