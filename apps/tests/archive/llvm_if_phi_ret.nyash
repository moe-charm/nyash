// Archived: superseded by apps/tests/phi_if_merge_ret.nyash for LLVM line
// (prints canonical Result: <n> for VM/LLVM comparison)
function main(args) {
  local c = 1
  local x
  if c == 1 {
    x = 10
  } else {
    x = 20
  }
  return x
}
