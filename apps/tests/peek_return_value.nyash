static box Main {
  main(args) {
    // Use match as an expression to produce a value
    local d = "1"
    local v = match d {
      "0" => 0,
      "1" => 1,
      _ => 0
    }
    // Show the result and finish
    local console = new ConsoleBox()
    console.println(v)
    return 0
  }
}
