// Simple loop + PHI merge, prints canonical Result line for VM/LLVM comparison.
static box Main {
  main() {
    local i = 1
    local sum = 0
    loop (i <= 5) {
      if (i % 2 == 1) { sum = sum + i } else { sum = sum + 0 }
      i = i + 1
    }
    print("Result: " + sum)
    return 0
  }
}

