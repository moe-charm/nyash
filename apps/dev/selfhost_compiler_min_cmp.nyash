using selfhost.compiler.main as CompilerMod

static box Main {
  main(){
    local args = new ArrayBox()
    args.push("--compiler-track")
    args.push("--min-json")
    args.push("--min-json-cmp")
    args.push("--emit-mir")
    CompilerMod.Main.main(args)
    return 0
  }
}
