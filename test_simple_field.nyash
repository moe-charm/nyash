// Simpler test for field access MIR
static box Main {
    init { obj }
    
    main() {
        me.obj = "test"
        local field_value
        
        // This should trigger field access parsing
        field_value = me.obj
        print(field_value)
        return "Done"
    }
}