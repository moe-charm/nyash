//! box_call.rs — BoxCall bridges
//!
//! Extraction target: builtin Array fast‑paths currently embedded in call flow.

use super::super::*;

impl MirInterpreter {
    // Map/Array fast-paths removed (Phase 15.7). Use normal dispatch.

    // StringBox fast-paths removed (Phase 15.7). Use normal dispatch.
}
