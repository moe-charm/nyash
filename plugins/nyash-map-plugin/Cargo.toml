[package]
name = "nyash-map-plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[features]
default = []
host-handle = []

[dependencies]
once_cell = "1.20"


hako_core_map = { path = "../../crates/hako_core_map" }
