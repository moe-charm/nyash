[package]
name = "nyash-filebox-plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]  # 動的ライブラリとしてビルド

[dependencies]
# 最小限の依存関係のみ

[features]
default = []

# プロファイル設定
[profile.release]
lto = true
strip = true
opt-level = "z"  # サイズ最適化