[package]
name = "nyash-json-plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
once_cell = "1.20"
serde_json = "1.0"
hako_abi_impl = { path = "../../crates/hako_abi_impl" }

[build-dependencies]
cc = "1.0"

## Note: Profiles must be specified at the workspace root (Cargo.toml).
## Defining them here causes cargo warnings and has no effect.
