[package]
name = "nyash-console-plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
once_cell = "1.20"

[profile.release]
lto = true
strip = true
opt-level = "z"
