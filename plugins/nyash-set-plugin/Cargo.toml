[package]
name = "nyash-set-plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
hako_abi_impl = { path = "../../crates/hako_abi_impl" }
once_cell = "1.20"
