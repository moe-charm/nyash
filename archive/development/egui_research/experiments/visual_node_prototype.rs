//! Placeholder binary for the historical visual node prototype.
//!
//! The original experiment file was dropped during refactors, but the
//! `Cargo.toml` entry still expects a compilable stub so that workspace
//! tooling such as `cargo fmt` can traverse all binaries without errors.

fn main() {
    // Keep the binary minimal; the real implementation lived outside this repo.
}
