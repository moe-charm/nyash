static box Main {
  main(args) {
    local vm = new MiniVm()
    local s = "value\":12}"
    local pos = s.indexOf("\":") + 2
    local d = vm.read_digits(s, pos)
    print(d.length())
    print(d)
    return 0
  }
}
