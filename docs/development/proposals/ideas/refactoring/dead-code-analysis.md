# Dead Code Analysis (2025-10-04)

## Overview

調査日: 2025-10-04
調査者: Claude Code
対象: Rust codebase の `#[allow(dead_code)]` 注釈

## Summary Statistics

- **Total files with dead_code**: 40 files
- **Top 2 files**:
  - `src/mir/optimizer.rs`: 11 annotations
  - `src/mir/builder/utils.rs`: 11 annotations

## Categorization

### 1. Future Optimization Passes (src/mir/optimizer.rs)

**Status**: ❌ **DO NOT DELETE** - Reserved for future implementations

Functions:
- `force_plugin_invoke()` - Plugin invocation normalization
- `normalize_python_helper_calls()` - Python helper normalization
- `normalize_legacy_instructions()` - Legacy instruction handling
- `normalize_ref_field_access()` - Reference field access normalization
- `diagnose_unlowered_type_ops()` - Type operation diagnostics
- `diagnose_legacy_instructions()` - Legacy instruction diagnostics
- `map_type_name()` - Type name mapping utility
- `opt_debug()`, `opt_debug_enabled()` - Debug utilities
- `resolve_type_from_value()` - Type resolution from value
- `instruction_to_key()` - Instruction key generation

**Rationale**: これらは将来の最適化パスのために準備されている。削除すると再実装が必要になる。

### 2. Future MIR Builder Features (src/mir/builder/utils.rs)

**Status**: ❌ **DO NOT DELETE** - Reserved for future features

Functions:
- `local_recv()`, `local_arg()`, `local_cmp_operand()`, `local_field_base()`, `local_cond()` - LocalSSA operations
- `emit_type_check()`, `emit_cast()` - Type operations (TypeOp instruction support)
- `emit_weak_new()`, `emit_weak_load()` - WeakRef operations
- `emit_barrier_read()`, `emit_barrier_write()` - Memory barrier operations

**Rationale**: これらは将来の言語機能（型チェック、WeakRef、メモリバリア）のために必要。

### 3. Plugin Development Utilities

Files with dead_code in plugins:
- `plugins/nyash-python-plugin/src/pytypes.rs`: 7 annotations
- `plugins/nyash-filebox-plugin/src/state.rs`: 6 annotations
- `plugins/nyash-filebox-plugin/src/tlv_helpers.rs`: 5 annotations
- `plugins/nyash-net-plugin/src/state.rs`: 3 annotations

**Status**: ⚠️ **REVIEW NEEDED** - Plugin-specific utilities may be removable

**Recommendation**: Plugin作者に確認。実際に使われていないなら削除可能。

### 4. Archived/Legacy Code

Files in archive/docs:
- `docs/archive/phases/phase-9/phase_9_75g_bid_ffi_abi_alignment.md`: 4 annotations (マークダウン内のコード例)
- `archive/jit-cranelift/src/jit/rt.rs`: 3 annotations

**Status**: ✅ **CAN DELETE** - Archived code can be removed

**Recommendation**: `archive/` ディレクトリは削除しても問題ない（git historyに残る）。

## Recommendations

### Immediate Actions (Safe)

1. ✅ **Archived code deletion**
   ```bash
   # archive/ ディレクトリ全体を削除（git historyに残る）
   git rm -r archive/
   ```

### Future Actions (Requires Judgment)

2. ⚠️ **Plugin utilities review**
   - Plugin作者に確認
   - 実際に使われていないユーティリティを削除

3. ❌ **DO NOT touch core features**
   - `src/mir/optimizer.rs` の最適化パス
   - `src/mir/builder/utils.rs` の将来機能

## Lessons from Previous Attempt

**2025-10-04の失敗事例**:
- "legacy" という名前のファイル（6ファイル、1,385行）を削除しようとした
- 結果: 15+のコンパイルエラー（`reg_store`, `handle_call` などはVMコア機能だった）
- 教訓: **ファイル名だけで判断してはいけない**

## Safe Refactoring Strategy

1. **調査**: まず依存関係を完全に理解
2. **並行実装**: 新しい実装を先に作る
3. **段階的移行**: 呼び出し箇所を少しずつ移行
4. **最後に削除**: 完全に不要になってから削除

## Total Potential Cleanup

- ✅ **Immediate**: archive/ ディレクトリ（数ファイル）
- ⚠️ **Maybe**: Plugin utilities（要確認）
- ❌ **Never**: Core optimizer/builder utilities（将来用）

## Conclusion

**40ファイルのdead_code注釈のうち、安全に削除できるのはarchiveのみ。**

他は将来の機能実装のために必要。無理に削除しようとすると、前回のような失敗を繰り返す。

---

Generated by: Claude Code (Anthropic)
Date: 2025-10-04
